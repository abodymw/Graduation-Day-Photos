Drop the two graduate cover photos here, named exactly:

- `graduate1.jpg`
- `graduate2.jpg`

(JPG or PNG both work — just keep these filenames, or update the `src` attributes in `index.html` to match whatever you use.)
